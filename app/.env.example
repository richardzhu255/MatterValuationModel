# Optional override if the brain API is hosted on a different origin.
# Leave unset for local Vite (proxy → :8790) and for same-origin Vercel deploys.
# Example: https://meridian-api.fly.dev
VITE_API_BASE_URL=
